import type { NextConfig } from 'next';

const config: NextConfig = {
  reactStrictMode: true,
  // CSS modules are enabled by default; no extra config needed.
  // Image optimisation stays on for the lifestyle photography that Real Wealth will supply later.
};

export default config;

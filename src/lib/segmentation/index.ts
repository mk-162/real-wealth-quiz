export { segment, buildQuestionList } from './engine';
export type { SegmentOutcome } from './engine';
export { assignSegment, upgradeSegment, rules } from './rules';
export type {
  GatingAnswers,
  SegmentId,
  SegmentationResult,
  QuestionId,
  MatrixCell,
  SegmentMatrix,
  IncomeBand,
  EstateBand,
  WorkStatus,
  HouseholdTag,
} from './types';
